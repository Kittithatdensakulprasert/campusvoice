// Feature 5: Search + Dashboard — ไผ่
//   SearchBar.jsx    — search input + dropdown filter
//   StatsCard.jsx    — stat summary card
//   PieChart.jsx     — Chart.js pie chart (issues by category)
//   BarChart.jsx     — Chart.js bar chart (issues by status)
//
// Feature 6: Admin Panel — mark
//   AdminPage.jsx        — หน้า admin panel
//   IssueTable.jsx       — ตารางจัดการปัญหา
//   StatusDropdown.jsx   — dropdown เปลี่ยน status

export default function AdminPlaceholder() {
  return <div>Admin components — Feature 5 (ไผ่) &amp; 6 (mark)</div>;
}
