// Feature 1: Auth — มิว
// สร้างไฟล์เหล่านี้ใน folder นี้:
//   LoginPage.jsx     — หน้า login form
//   RegisterPage.jsx  — หน้า register form
//   ProtectedRoute.jsx — wrapper สำหรับ route ที่ต้อง login ก่อน

export default function AuthPlaceholder() {
  return <div>Auth components — Feature 1 (มิว)</div>;
}
