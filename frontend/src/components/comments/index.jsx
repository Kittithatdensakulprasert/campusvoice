// Feature 4: Comment — หนุ่ม
//   CommentList.jsx  — แสดงรายการ comment
//   CommentItem.jsx  — comment แต่ละอัน
//   CommentForm.jsx  — form เพิ่ม comment

export default function CommentsPlaceholder() {
  return <div>Comment components — Feature 4 (หนุ่ม)</div>;
}
